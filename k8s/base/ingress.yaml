apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: order-service
  annotations:
    kubernetes.io/ingress.class: traefik
spec:
  rules:
  - host: REPLACEME
    http:
      paths:
      - path: /
        backend:
          servicePort: http
          serviceName: order-service
