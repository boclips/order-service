slack:
  signingSecret: a-fake-secret
server:
  use-forward-headers: true
pubsub:
  consumerGroup: order-service
video-service:
  base-url:
  access-token-uri:
  client-id:
  client-secret:
spring:
  http:
    encoding:
      charset: UTF-8
      force: true